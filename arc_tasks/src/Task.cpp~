#include "Task.h"

using namespace arc_tasks;

Task::Task(int task, int priority){
  this->task = task;
  this->priority = priority;
}

int Task::get_task(){
  return task;
}

int Task::get_priority(){
  return priority;
}
