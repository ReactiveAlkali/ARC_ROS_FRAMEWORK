#include "ros/ros.h"
#include "Bot_Brain.h"

int main(int argc, char **argv) {
    ros::init(argc, argv, "bot_brain_node");
    arc_behaviour::Bot_Brain bot_brain;
    bot_brain.run();
    return 0;
}
